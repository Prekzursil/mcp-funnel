interface:
  display_name: "Mcp Funnel"
  short_description: "Repo-specific patterns and workflows for mcp-funnel"
  default_prompt: "Use the mcp-funnel repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true